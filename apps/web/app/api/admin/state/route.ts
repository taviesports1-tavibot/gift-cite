import { NextResponse } from "next/server";
import { isAdmin } from "@/lib/admin-auth";
export async function GET(){if(!await isAdmin())return NextResponse.json({error:"unauthorized"},{status:401});const url=process.env.REALTIME_SERVER_URL;if(!url)return NextResponse.json({error:"not configured"},{status:503});const [health,state,mappings,catalog]=await Promise.all([fetch(`${url}/health`,{cache:"no-store"}),fetch(`${url}/api/state`,{cache:"no-store"}),fetch(`${url}/api/mappings`,{cache:"no-store"}),fetch(`${url}/api/catalog`,{cache:"no-store"})]).catch(()=>[]);if(!health||!state||!mappings||!catalog)return NextResponse.json({error:"offline"},{status:502});return NextResponse.json({health:await health.json(),state:await state.json(),mappings:await mappings.json(),catalog:await catalog.json()});}
